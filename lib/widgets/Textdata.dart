import 'package:flutter/material.dart';

class TextData {
  String content;
  Offset position;
  String fontFamily;
  double fontSize;
  Color fontColor;

  TextData(this.content, this.position, this.fontFamily, this.fontSize,
      this.fontColor);
}
